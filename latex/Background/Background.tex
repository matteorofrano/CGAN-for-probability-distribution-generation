\chapter{Background}


\section{CGAN formulation}
GANs are often formalized as a minimax game, where the generator (G) seeks to minimize the value function V while the discriminator (D) aims to maximize it:
\begin{equation}
    \label{eq:GAN minimax}
\min _G \max _D V(D, G)=\mathbb{E}_{\boldsymbol{x} \sim p_{\text {data }}(\boldsymbol{x})}[\log D(\boldsymbol{x})]+\mathbb{E}_{\boldsymbol{z} \sim p_{\boldsymbol{z}}(\boldsymbol{z})}[\log (1-D(G(\boldsymbol{z})))]
\end{equation}

The discriminator outputs a value $D(x)$, 
which represents the probability that $x$ is the real vector containing mean and variance of the normal distribution.
Its goal is to correctly classify the real parameters as real and generated parameters as fake,
effectively maximizing the likelihood of the observed data.
To measure this, we use cross-entropy loss, as is common in deep learning,
expressed as $plog(1-p)$. 
For real parameters, the true label $p$ equals 1, 
while for generated parameters the label is 0.
With this setup, the discriminator's objective becomes:
\begin{equation}
    \label{eq:discriminator objective}
    \max _D V(D)=\mathbb{E}_{\boldsymbol{x} \sim p_{\text {data }}(\boldsymbol{x})}[\log D(\boldsymbol{x})]+\mathbb{E}_{\boldsymbol{z} \sim p_{\boldsymbol{z}}(\boldsymbol{z})}[\log (1-D(G(\boldsymbol{z})))]
\end{equation}

where $\mathbb{E}_{\boldsymbol{x} \sim p_{\text {data }}(\boldsymbol{x})}[\log D(\boldsymbol{x})]$ define how good the discriminator recognize real parameters.
and $\mathbb{E}_{\boldsymbol{z} \sim p_{\boldsymbol{z}}(\boldsymbol{z})}[\log (1-D(G(\boldsymbol{z})))]$ define how good the discriminator recognizes generated parameters.\\
The generator's objective is to produce distribution parameters that maximize $D(x)$, pushing the discriminator to classify them as real:
\begin{equation}
    \label{eq:generator objective}
\min _G V(G)=\mathbb{E}_{\boldsymbol{z} \sim p_{\boldsymbol{z}}(\boldsymbol{z})}[\log (1-D(G(\boldsymbol{z})))]
\end{equation}

Where $\mathbb{E}_{\boldsymbol{z} \sim p_{\boldsymbol{z}}(\boldsymbol{z})}[\log (1-D(G(\boldsymbol{z})))]$ define how good the generator is in fooling the discriminator.\\

In the Conditional GAN (CGAN), the generator learns to generate a fake sample with a specific condition, in our case the past trajectory, rather than a generic sample from unknown noise distribution.
The CGAN objective function is given by:
\begin{equation}
    \label{eq:CGAN minimax}
    \min _G \max _D V(D, G)=\mathbb{E}_{\boldsymbol{x} \sim p_{\text {data }}(\boldsymbol{x})}[\log D(\boldsymbol{x} \mid \boldsymbol{y})]+\mathbb{E}_{\boldsymbol{z} \sim p_{\boldsymbol{z}}(\boldsymbol{z})}[\log (1-D(G(\boldsymbol{z} \mid \boldsymbol{y})))]
\end{equation}

Academic findings highlighted in \cite{background:gan_time_series} suggest that one of the principal failure modes of GANs 
arises when the generator converges to a parameter configuration that yields only a limited variety of outputs.
Existing research indicates that the occurrence of mode collapse is attributed to the establishment of a local 
equilibrium within the minimax optimization framework.
A further critical challenge involves the vanishing gradient problem,
which emerges when the discriminator becomes overly proficient,
causing the generator's gradients to approach zero and consequently impeding its learning process.

\section{Data generation process}

for each trajectory $j = 1, ..., J$ we define 
the SDE of the log-price $X_t^j$ as:
\begin{equation}
    \label{log-price SDE}
    \begin{aligned}
       &dX_t^j = -\frac{1}{2}\sigma_j^2 dt + \sigma_j dW_t\\
         &\text{with } X_0^j = \log(S_0^j)
    \end{aligned}
    \end{equation} 

Where $S_0^j$ is the raw price at time $0$ of trajectory $j$, $\sigma_j$ is the volatility of trajectory $j$ and $W_t$ is a standard Brownian motion.\\
$x_0^j$ and $\sigma_j$ are randomly sampled for each trajectory such that 
$x_0^j \in [0, 10]$ and $\sigma_j \in (0.0, 1.0]$.\\

In this context our condition label $y_j$ for a general data observation $j$ is given by
a vector containing the trajectory $y_j=[X_0^j, X_{dt}^j, X_{2dt}^j, ..., X_{Ndt}^j]$.
So our condition data matrix $Y$ has shape $J\times N$ where $J$ is the number of trajectories (data points)
 and $N$ is the number of time steps in each trajectory.\\
 
 The SDE is discretized over a uniform time space $N dt = T$ obtaining the following form
 adopted for the simulation of the trajectories:
 \begin{equation}
    X_{t+dt}^j = X_0^j -\frac{1}{2}\sigma_j^2 dt + \sigma_j \sqrt{dt} Z_t
 \end{equation}
 Where $Z_t \sim \mathcal{N}(0,1)$.\\
The procedure to build the condition data matrix $Y$ is summarized in Algorithm \ref{alg:y condition}.

\begin{algorithm}[H]
\caption{Algorithm for the condition data matrix $Y$ creation}
\label{alg:y condition}
\begin{algorithmic}[1]

\STATE Define the number of simulation $J$.
\STATE Initialize an empty data matrix $Y$ of shape $J\times N$.
\STATE Set iteration counter $j \leftarrow 0$.
\REPEAT
    \STATE Randomly sample $X_0^j \in [0.0, 10.0]$ and $\sigma_j \in (0.0, 1.0]$.
    \STATE Evolve $X_t^j$ based on the discretized SDE for $0, dt, ..., Ndt$ steps.
    \STATE Store the trajectory $[X_0^j, X_{dt}^j, X_{2dt}^j, ..., X_{Ndt}^j]$ in the $j$-th row of $Y$.
    \STATE Increment counter $j \leftarrow j + 1$.
\UNTIL{$j = J$}
\RETURN $Y$
\end{algorithmic}
\end{algorithm}



\subsection{Distribution's parameters as output}
In $T=N dt$ we have the following information: the final log-price $X_{Ndt}^j$ of each trajectory $j$ and the corresponding volatility $\sigma_j$.
The goal of the cGAN is to learn the distribution of the next time interval $Ndt + \Delta t$ 
which is a normal distribution $\mathcal{N}(X_{Ndt}^j - \frac{1}{2}\sigma_j^2 \Delta t, \sigma_j \sqrt{\Delta t})$.\\
So the data matrix $X$ which has to be generated, that in classical implementations of GANs are images, is in this case 
a vector containing the parametrization of the distribution. In this case a vector of two elements:
the mean and the standard deviation.

\subsection{Discretized distribution through bins as output}
An alternative approach to represent the distribution at time $T + \Delta t$, is to approximate it through bins.
In this case the data matrix $X$ is a vector of dimension $B$ where $B$ is the number of bins' ranges adopted for the approximation.
\begin{itemize}
    \item \textbf{local constraints on the bin representation}: in this case, for each trajectory $j$,
    the normal distribution is discretized by using a common bin representation obtained by taking as extreme values $\mu_j \pm 4\sigma_j$. Then $100$ evenly spaced bins edges are created between these two extreme values. 
    In order to compute the density inside each bin range, bins' edges in matrix $B$ are standardized using the mean and standard deviation of the resulting normal distribution for sample $j$,
    then the standard cumulative distribution function is applied to each bin edge, and finally the difference between two consecutive bin edges is computed.\\
    In this simple scenario it is easy to observe that every sample is the same discretized standard normal distribution.
    \item \textbf{global constraints on the bin representation}: in this case, for each trajectory $j$,
    the normal distribution is discretized by taking as extreme values the minimum value of the vector obtained by $\mu - 4\sigma$ and the maximum value of the vector
    obtained by $\mu + 4\sigma$. Then $100$ evenly spaced bins edges are created between these two extreme values. 
    Again, in order to compute the density inside each bin range, bins' edges in matrix $B$ are standardized using the mean and standard deviation of the resulting normal distribution for sample $j$,
    then the standard cumulative distribution function is applied to each bin edge, and finally the difference between two consecutive bin edges is computed.\\
    A drawback of this method is that the output vectors tends to be sparse since many bins will have a density equal to zero.
\end{itemize}

\subsection{Conditional expected value of the bin as output}
Each element of the vector represents the expected value given that the value falls inside that bin range, so 
$E[ X_{Ndt}^j | b_i \leq X_{Ndt}^j \leq b_{i+1}]$ for $X_{Ndt}^j \sim \mathcal{N}(X_{Ndt}^j - \frac{1}{2}\sigma_j^2 \Delta t, \sigma_j \sqrt{\Delta t})$, in the following description
we just define the mean parameter as $\mu_{X^j}$ and the standard deviation parameter as ${\sigma_{X^j}}$.
To create this representation we first define the bin edges by taking as min and max values the $ \pm 4 \sigma_{X^j}$ values coming from the normal distribution previously defined.
Then we create $B$ bins uniformly spaced between the min and max values.
For each bin range $B_i$ defined by the edges $[b_{i}, b_{i+1}]$,
we compute the expected value for each bin by using the following formula:
\begin{equation}
    \mathbb{E}[Z \mid a_i \leq Z \leq a_{i+1}] = \frac{\phi(a) - \phi(a_{i+1})}{\Phi(a) - \Phi(a_{i+1})}
\end{equation}
where $a_{i}$ and $a_{i+1}$ are the standardized bins, $\phi(z)=\frac{1}{\sqrt{2\pi}} e^{-z^2/2}$ is the standard normal probability density function and 
$\Phi(z) = \int_{-\infty}^{z} \phi(t) dt$. Finally, the expected value of $Z$ inside the two standardized bins is converted back to its original scale by multiplying it by ${\sigma_{X^j}}$ and adding $\mu_{X^j}$.



\section{The Kolmogorov-Smirnov test}
The Kolmogorov-Smirnov (KS) as defined in \cite{background:ks_test}, is a goodness of fit test that evaluates whether an empirical distribution is consistent with a fully specified continuous reference distribution. 
Let $F(x)$ denote the hypothesized cumulative distribution function (CDF), 
and let $F_{n}(x)$ be the empirical CDF constructed from a sample of size $n$. 
The KS statistic is defined as:
\begin{equation}
    D_{n}=\sup_{x}\lvert F_{n}(x)-F(x)\rvert
\end{equation}
representing the maximum vertical deviation between the empirical and theoretical CDFs.
Under the null hypothesis $H_{0}: F_{n}(x)=F(x)$
the scaled distribution $\sqrt{n}D_{n}$ converges to the Kolmogorov distribution, enabling computation of p-values via:
\begin{equation*}
    P(\sqrt{n}D_{n}\le t)=1-2\sum_{k=1}^{\infty}(-1)^{k-1}e^{-2k^{2}t^{2}}
\end{equation*}
Large values of $D_{n}$ lead to rejection of $H_{0}$, indicating
incompatibility between the empirical and theoretical distributions.\\

When applying the KS framework to generated outputs from the proposed GAN, the empirical object is a vector of bin probabilities
$(p_{1},\dots,p_{K})$  These represent a discretized approximation of the model’s PDF. To construct the empirical CDF, 
define cumulative probabilities:
\begin{equation*}
    F_{\text{GAN}}(x_{k})=\sum_{i=1}^{k}p_{i}
\end{equation*}

where $x_k$ indexes the bin boundaries. For comparison with a normal distribution, compute the theoretical CDF
$F_{\mathcal N}(x_{k})$ at the same grid. The KS statistic becomes:
\begin{equation*}
    D=\max_{k}\lvert F_{\text{GAN}}(x_{k})-F_{\mathcal N}(x_{k})\rvert
\end{equation*}

This yields a valid discrepancy measure, although interpreting p-values requires specifying an effective sample size, since the GAN vector itself is not an empirical sample.
Nonetheless, $D$ remains a rigorous, distribution-free indicator of alignment between the GAN-generated distribution and the target normal CDF.
We can actually compute the p\-value of the test using the survival function of the kolmogorov distribution since $P(D_n \geq \hat{D_n} | H_0)$.




\section{Possible improvement to CGAN}
Different strategies have been adopted to improve training stability and convergence 
of GANs.
\begin{itemize}
    \item \textbf{Wasserstein GAN}: WGAN \cite{background:wgan} introduces the Wasserstein distance (Earth Mover's Distance) as a new loss function for GANs. This distance metric provides a more meaningful measure of the difference between the real and generated data distributions,
     leading to improved training stability and convergence. The WGAN objective function is given by $\min _G \max _{D \in \mathcal{D}} E_{\boldsymbol{x} \sim p_d}[D(\boldsymbol{x})]-E_{\widehat{\boldsymbol{x}} \sim p_g}[D(\widehat{\boldsymbol{x}})]$.
     \item \textbf{DRAGAN}: this framework was introduced to address these challenges by incorporating a gradient penalty directly into the GAN architecture.
    The authors in \cite{background:dragan} observed that the local equilibria frequently manifest steep discriminator gradients in the vicinity of certain real data samples.
    They further demonstrated that such degenerate local equilibria can be mitigated by applying a gradient penalty to the objective function, the method adopted is called DRAGAN.
    \item A tip suggested in \cite{background:gan_time_series} to do not use batch normalization after adding the gradient penalty in order to improve model performances.
    \item in \cite{background:rcgan} both the generator and discriminator are RNN or LSTM networks adopted to better capture the sequential dependencies in time series data.
\end{itemize}


\section{training process}
During the training process of the proposed CGAN, a preprocess step is adopted 
depending on the output representation selected. The following setup for the experiments has been used:\\
\begin{itemize}
    \item \textbf{Initial price range}: $X_0 \in (0.0, 1.0)$
    \item \textbf{Drift range}: $\mu \in (0.0, 0.0)$
    \item \textbf{Volatility range}: $\sigma \in (0.001, 1.0)$
    \item \textbf{Time horizon}: $T = 1.0$ year
    \item \textbf{Number of time steps}: $N = 252$
    \item \textbf{Number of simulated paths}: $J = 100{,}000$
    \item \textbf{n-steps ahead}: $\Delta_t = 10$
\end{itemize}

The experimental setup is based on a synthetic data generation process designed to emulate realistic financial price dynamics exploiting Geometric Brownian motion behaviour
for training and evaluating the GAN model. 
Asset price trajectories are simulated over a fixed time horizon of one year, discretized into $N=252$ time steps corresponding to daily observations.
For each experiment, $J=100{,}000$ independent paths are generated, ensuring sufficient sample diversity and statistical robustness.
The initial asset price $X_0$ is drawn from a uniform range $(0.0,1.0)$, while the drift parameter $\mu$ is fixed at zero to isolate the model's ability to learn volatility-driven dynamics.
The volatility parameter $\sigma$ is sampled from a wide range $(0.001,1.0)$ to capture varying market conditions, from near-deterministic to highly stochastic regimes.
The PDF to be learned refers to the $10$-th step ahead distribution, that is the distribution of asset prices at time $T + 10$ (or $N dt + 10$) given information up to time $T$.

\subsection{mean and std as output}
When the output representation is the mean and standard deviation of the normal distribution,
a standardization preprocess is applied to both the condition data matrix $Y$ and the target data matrix $X$.
This involves centering the data to have zero mean and scaling it to have unit variance.\\

In this case, the distance between the real and generated distribution is measured by the norm $L_2$ between the real and generated parameters vectors.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Background/images/mean_std_cgan.png}
    \caption{Distance between real and generated parameters of the distribution over epochs.}
    \label{mean_std_distance}
\end{figure}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/error_mean_std.png}
    \caption{Error distribution between real and generated parameters.}
    \label{mean_std_error}
\end{figure}


\subsection{conditional expected values as output}
When the output representation is the conditional expected values inside each bin of the normal distribution,
a standardization preprocess is applied to both the condition data matrix $Y$ and the target data matrix $X$.
and the norm $L_2$ between the real and generated vectors is used to measure the distance between the real and generated distribution.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/bins_ev_cgan.png}
    \caption{Distance between real and generated conditional expected values.}
    \label{ev_distance}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/error_ev_bins.png}
    \caption{Error distribution between real and generated conditional expected values.}
    \label{ev_error}
\end{figure}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/bins_ev_sample.png}
    \caption{Sample of real and generated conditional expected values of the normal distribution.}
    \label{bins_local_sample}
\end{figure}



\subsection{bins probabilities as output}
When the output representation is the bins probabilities of the normal distribution,
a log-transform preprocess is applied to the target data matrix $X$.
This involves applying the natural logarithm to each element of the data matrix.\\
In this case the distance between the real and generated distribution is measured by the Shennon entropy
between the real and generated bins probabilities vectors.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/bins_distribution_cgan.png}
    \caption{Distance between real and generated probability density function of the standard normal distribution over epochs.}
    \label{bins_local_distance}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/error_bins.png}
    \caption{Error distribution between real and generated bins probabilities of the standard normal distribution.}
    \label{bins_local_error}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/bins_sample.png}
    \caption{Sample of real and generated bins probabilities of the standard normal distribution.}
    \label{bins_local_sample}
\end{figure}

A Kolmogorov-Smirnov test has been performed between 100 real and generated bins probabilities
to evaluate the goodness of fit of the generated distributions.
The resulting p-values are on average around $0.99$ meaning that we cannot reject the null hypothesis
that the two distributions are the same.\\

The same procedure is applied when global bin representation is used.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/bins_global_distribution_cgan.png}
    \caption{Distance between real and generated probability density function using global bins.}
    \label{bins_global_distance}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/error_bins_global.png}
    \caption{Error distribution between real and generated bins probabilities using global bins.}
    \label{bins_global_error}
\end{figure}

Also in this case the Kolmogorov-Smirnov test has been performed between 100 real and generated bins probabilities, and
the resulting p-values are rejected 5\% of times, meaning that we can reject the null hypothesis
that the two distributions are the same.\\


\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Background/images/bins_global_sample.png}
    \caption{All samples are drawn such that $X_0 = 0.8$ and $\sigma = 0.4$.\\
    \textbf{a - } Sample of real and generated bins probabilities using global bins over 1000 samples.\\
    \textbf{b - } Sample of real and generated bins probabilities using global bins over 1 sample.}
    \label{bins_global_sample}
\end{figure}

As can be seen from figure \ref{bins_global_sample} the main problem faced when using global bins
is that during the training phase the generator is exposed to bins values which are very far away from each other,
since they are based on the min and max values of the entire dataset, which can approach to milions of observations.
This makes the shape of the generated distribution very concentrated to few bins on average, as we can see from the spikes of the generated distributions.
However, when looking at few trajectories for which we would like to generate the future final price distribution,
the real distributions are not so concentrated since the bins are computed from the min and max values of that specific trajectories.
This leads to a poor quality of the generated distributions when global bins are used.



\section{Possible improvements to target modeling}
The main issue faced when modeling the target distribution is how to
actually define the output representation of the distribution to be learned.
If local bins are used, by exploiting values at $\mu \pm 4\sigma$ for each trajectory,
then the resulting bins probabilities vectors are all the same for each trajectory, since
a standardization procedure is needed to compute the bins probabilities. In fact,
for each trajectory the minimum bin edge will be $-4$ since $(\mu - 4\sigma - \mu)/\sigma = -4$
and the maximum bin edge will be $4$ since $(\mu + 4\sigma - \mu)/\sigma = 4$. So at the end, 
the same standard normal distribution is discretized for each trajectory.\\

If global bins are used, then a trade-off between resolution and coverage is faced.
The resolution is the ability to capture fine-grained structure in the predictive distribution, while the coverage ensures that discretization bins meaningfully encompass the observed data.
When globally defined bins are employed, resolution is degraded because the probability mass is often concentrated within a narrow subset of the overall range, rendering much of the discretization uninformative.\\

Some possible solutions are:
\begin{itemize}
    \item Model standardized log-returns rather than absolute prices.
    \item Predict the values corresponding to a fixed set of quantiles. 
    The model generates the outcome values associated with specified probability levels for example $[0.01, 0.1, 0.25, 0.5, 0.75, 0.9, 0.99]$.
    Usually this approach is used in quantile regression where the loss function is the quantile loss, which is specifically designed to provide consistent estimation of conditional quantiles.
    \item The generated output can be a single scalar expected value of the next time step, and then,
    at inference time, the distribution can be recovered by repeatedly evaluating the generator for a fixed input trajectory while varying the random noise.
    The collection of generated outputs, for example 1000 samples, constitutes an empirical approximation of the conditional predictive distribution.
    \item Add a term to the generator's loss that penalizes it if the mean and variance of a batch of generated samples don't match the mean and variance of the "real" theoretical samples.
\end{itemize}


Possible approach is to compute the quantiles of the real normal distribution and use them as output representation.